#include "main.h"

/**
 * print_alphabet_x10 -function to print abc 10 times
 *
 * Return: Always 0
 */
void print_alphabet_x10(void)
{
	char c, 1;

	for (i = 0; 1 <= 9; i++)
	{
		for (c = 'a'; c <= 'z'; j++)
		{
			_putchar(c);
		}
		_putchar('\n');
	}
}
